# GitHub Actions CI — Continuous Integration (Lint, Test, Validate)
# Runs on every push and PR to main. Fast feedback loop.
# ⌬ Auto-generated by hydrate.py — DO NOT EDIT DIRECTLY

name: "MYTH CI"

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # ─── Stage 1: Validate ──────────────────────────────────────────
  validate:
    runs-on: ubuntu-latest
    name: "⌬ Validate"
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Dependencies
        run: uv sync --all-extras
        env:
          UV_LINK_MODE: "copy"

      - name: Run Validation
        run: uv run python scripts/ci_orchestrator.py validate --verbose

  # ─── Stage 2: Lint ──────────────────────────────────────────────
  lint:
    runs-on: ubuntu-latest
    name: "🔍 Lint"
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Dependencies
        run: |
          uv sync --all-extras
          uv pip install ruff
        env:
          UV_LINK_MODE: "copy"

      - name: Run Linter
        run: uv run python scripts/ci_orchestrator.py lint --verbose

  # ─── Stage 3: Test ──────────────────────────────────────────────
  test:
    runs-on: ubuntu-latest
    needs: validate
    name: "🧪 Test"
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Dependencies
        run: uv sync --all-extras
        env:
          UV_LINK_MODE: "copy"

      - name: Run Tests
        run: uv run python scripts/ci_orchestrator.py test --verbose

      - name: Upload Test Errors
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-errors
          path: tests/test_errors.txt
          retention-days: 7
