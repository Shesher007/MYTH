# MYTH Desktop — Void Linux xbps-src Template (Binary)
# Build: ./xbps-src pkg myth
# Install: xbps-install myth

pkgname=myth
version=1.1.6
revision=1
short_desc="High-Performance Offensive Intelligence Engine (Binary)"
maintainer="Shesher Hasan <shesher0007@gmail.com>"
license="custom:proprietary"
homepage="https://github.com/shesher010/MYTH"
# Download the binary directly
distfiles="https://github.com/shesher010/MYTH/releases/download/v${version}/myth_${version}_amd64.deb"
checksum="SKIP" # Replace with actual checksum in production

depends="webkit2gtk openssl python3 gtk+3 libappindicator"

do_install() {
    vbin ${XBPS_SRCDIR}/MYTH_x86_64 MYTH
    # Icons and desktop files can be added here if bundled in the release
}

